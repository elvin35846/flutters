import 'package:flutter/material.dart';

abstract class AppColor {
  static const mainColor = Color.fromRGBO(3, 37, 65, 1);
  static const selectNavBarIconColor = Colors.white;
  static const unselectNavBarIconColor = Colors.grey;
}
